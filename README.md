# FitScale Gate Keeper — OTA firmware

Public host for compiled gate firmware `.bin` images (OTA self-update).
No source code, no secrets — the device key + WiFi live in the ESP32 NVS, not the binary.

Raw .bin URLs here are what you paste into a gym dashboard → Settings → Gate Firmware (OTA).
